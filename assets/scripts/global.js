'use strict';

// create an object to store items I'm using as global variables
const globalVars = {
  playerLogin: false,
  createGameSuccess: false,
  newestGameID: 0
};

module.exports = {
  globalVars,
};
